# 🚀 Render.com 部署指南

本指南將協助你將電影票房預測網站部署到 Render.com。

---

## 📋 準備工作

### 1. 確認檔案準備完成
確認以下檔案存在：
- ✅ `requirements.txt` - Python 套件清單（包含 gunicorn）
- ✅ `render.yaml` - Render.com 配置檔
- ✅ `app.py` - Flask 主程式

### 2. 建立 GitHub 儲存庫（如果還沒有）
```bash
# 在專案根目錄
git init
git add .
git commit -m "Initial commit for deployment"
git branch -M main
git remote add origin <你的 GitHub 儲存庫 URL>
git push -u origin main
```

---

## 🌐 部署步驟

### 方法一：使用 render.yaml 自動部署（推薦）

#### 步驟 1：登入 Render.com
1. 前往 [Render.com](https://render.com/)
2. 使用 GitHub 帳號登入

#### 步驟 2：連接 GitHub 儲存庫
1. 點擊 **New +** → **Blueprint**
2. 選擇你的 GitHub 儲存庫
3. 指定 `render.yaml` 所在目錄：`src/web/business/detail`
4. 點擊 **Apply**

#### 步驟 3：等待部署完成
- Render 會自動：
  - 安裝套件
  - 啟動 Flask 應用
  - 產生網址（如 `https://cinpos-boxoffice-web.onrender.com`）

#### 步驟 4：訪問網站
部署成功後，點擊 Render 提供的網址即可訪問！

---

### 方法二：手動建立 Web Service

如果不使用 render.yaml，可以手動設定：

#### 步驟 1：建立 Web Service
1. 登入 Render.com
2. 點擊 **New +** → **Web Service**
3. 連接 GitHub 儲存庫

#### 步驟 2：設定服務
填寫以下資訊：

| 設定項目 | 值 |
|---------|---|
| **Name** | `cinpos-boxoffice-web` |
| **Region** | `Singapore` (離台灣較近) |
| **Branch** | `main` |
| **Root Directory** | `src/web/business/detail` |
| **Runtime** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `gunicorn -w 4 -b 0.0.0.0:$PORT app:app` |
| **Plan** | `Free` |

#### 步驟 3：設定環境變數
在 **Environment** 頁籤新增：
- `FLASK_DEBUG` = `false`
- `SECRET_KEY` = （自動生成或手動輸入隨機字串）
- `PYTHON_VERSION` = `3.13`

#### 步驟 4：部署
點擊 **Create Web Service**，等待部署完成。

---

## ⚙️ 環境變數說明

| 變數名稱 | 必要性 | 說明 |
|---------|--------|------|
| `FLASK_DEBUG` | 選填 | 是否啟用 Debug 模式（生產環境建議 `false`） |
| `SECRET_KEY` | 必要 | Flask 密鑰（用於 session 加密） |
| `PYTHON_VERSION` | 選填 | Python 版本（預設使用 3.13） |

---

## 📊 部署後檢查

### 1. 檢查服務狀態
在 Render Dashboard 確認：
- ✅ 狀態顯示為 **Live**
- ✅ 沒有錯誤日誌

### 2. 測試網站
訪問 Render 提供的網址，確認：
- ✅ 首頁正常載入
- ✅ 電影詳細頁面可以顯示
- ✅ 圖表正常運作

### 3. 查看日誌
如果有問題，在 Render Dashboard 點擊 **Logs** 查看錯誤訊息。

---

## 🔄 更新部署

### 自動部署（推薦）
如果啟用 `autoDeploy: true`（預設已啟用），只需：
```bash
git add .
git commit -m "更新內容"
git push
```
Render 會自動偵測並重新部署。

### 手動部署
在 Render Dashboard：
1. 進入你的 Web Service
2. 點擊 **Manual Deploy** → **Deploy latest commit**

---

## ⚠️ 常見問題

### Q1: 部署失敗，顯示 "Module not found"
**A:** 確認 `requirements.txt` 包含所有必要套件，特別是 `gunicorn`。

### Q2: 網站載入很慢
**A:** 免費方案會在閒置 15 分鐘後休眠，首次訪問需要 30-60 秒喚醒。

### Q3: 靜態檔案（CSS/JS）無法載入
**A:** 確認 Flask 的 `static` 和 `templates` 資料夾結構正確。

### Q4: 如何查看錯誤日誌？
**A:** 在 Render Dashboard → 你的服務 → **Logs** 頁籤。

### Q5: 免費方案有什麼限制？
**A:**
- 閒置 15 分鐘後自動休眠
- 每月 750 小時免費運行時間
- 較慢的 CPU 和記憶體

---

## 🎯 下一步

部署成功後，你可以：
1. ✨ 設定自訂網域（需付費方案）
2. 📊 連接資料庫（如 PostgreSQL）
3. 🔄 設定 Cron Jobs 定期更新資料
4. 📧 設定通知（部署成功/失敗時發送郵件）

---

## 📚 相關資源

- [Render 官方文件](https://render.com/docs)
- [Flask 部署指南](https://flask.palletsprojects.com/en/3.0.x/deploying/)
- [Gunicorn 配置](https://docs.gunicorn.org/en/stable/settings.html)

---

## 💡 提示

- 建議先在本地測試 `gunicorn app:app` 確認能正常啟動
- 部署前確認所有檔案已提交到 GitHub
- 保持 `SECRET_KEY` 安全，不要提交到 Git

---

**祝部署順利！** 🎉
